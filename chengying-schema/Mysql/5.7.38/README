# Mysql
    mysql制品产品包示例：
    https://dtstack-opensource.oss-cn-hangzhou.aliyuncs.com/chengying/Mysql_5.7.38_centos7_x86_64.tar

#  Mysql schema说明
    Hive元数据使用mysql，具体版本是5.7.38，支持主备模式部署
    Mysql目下包含一个以mysql具体版本号命名的文件夹。
    文件夹下固定包含四个模块：
        1、config文件夹
        2、components文件
        3、schema配置文件
        4、README
    ##### config
    config目录下主要为Mysql包中包含的服务角色:
        1、mysql服务：主mysql，对外提供服务
        2、mysql_slave服务：从mysql，主要做主库mysql数据同步和备份
        
